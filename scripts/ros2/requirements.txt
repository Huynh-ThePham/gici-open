# Python dependencies for the ROS 2 UrbanNav bag tooling in scripts/ros2/.
#
#   urbannav_rrr_to_ros2.py, urbannav_gnss_bag_to_ros2.py, finalize_rinex_bag.py
#
# Install into the same interpreter you run the scripts with:
#   python3 -m pip install -r scripts/ros2/requirements.txt
#
# Pins reflect the versions these scripts were developed/validated against.
# rosbags reads ROS 1 bags and writes ROS 2 (rosbag2 v8) bags without a ROS
# install; numpy is used for image (bgr8->mono8) handling.
rosbags>=0.11,<1.0
numpy>=1.24,<2.0
